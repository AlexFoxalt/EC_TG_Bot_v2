# Copy this file to ".env" and adjust values as needed.
#
# IMPORTANT:
# - Keep real secrets out of git.
# - "latest" is convenient, but for stability consider pinning to a major version
#   (example: 17-alpine) and upgrading intentionally.

COMPOSE_PROJECT_NAME=ec_tg_bot_v2

# PostgreSQL Configuration
POSTGRES_IMAGE_TAG=latest
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=change_me_to_a_long_random_password

# Telegram Bot Configuration
TELEGRAM_TOKEN=your_telegram_bot_token_here

# Tapo Device Configuration
TAPO_EMAIL=your_tapo_email@example.com
TAPO_PASSWORD=your_tapo_password
TAPO_DEVICE_ID=your_tapo_device_id

# Application Configuration
NOTIFICATION_POLL_INTERVAL_SECONDS=5
POLL_INTERVAL_SECONDS=10

# Logging Configuration
LOG_LEVEL=INFO
LOGURU_COLORIZE=false
