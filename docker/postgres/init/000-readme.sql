-- This directory is automatically executed by the official Postgres image
-- *only on first initialization* of the data directory.
--
-- Put schema/bootstrap SQL here if you need it.
--
-- Intentionally empty by default.
SELECT 1;
